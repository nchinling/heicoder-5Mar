//Ex4
// Given an array of strings, use .map() to create an array that contains the length of each string:

const strings = ['hello', 'world', 'javascript'];

const lengths = strings.map(string => string.length)

console.log(lengths); // Output: [5, 5, 10]

